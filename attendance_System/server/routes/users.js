const router = require("express").Router();

router.get("/:userId", () => {});

router.put("/:userId", () => {});

router.patch("/:userId", () => {});

router.delete("/:userId", () => {});

router.get("/", () => {});

router.post("/", () => {});
